server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/api/auth/**
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/api/users/**
        - id: hostel-service
          uri: lb://hostel-service
          predicates:
            - Path=/api/hostels/**
        - id: guest-service
          uri: lb://guest-service
          predicates:
            - Path=/api/guests/**
        - id: billing-service
          uri: lb://billing-service
          predicates:
            - Path=/api/billing/**
        - id: notification-service
          uri: lb://notification-service
          predicates:
            - Path=/api/notifications/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

jwt:
  # Must match auth-service HS256 secret (ideally set via env var JWT_SECRET)
  secret: ${JWT_SECRET:XJ6M0mF6f4i3vYx8h9Q2zL7kN5sR1cW3uT8pAaBbCcDdEeFfGgHhIiJjKkLlMm==}
  # Endpoints that should NOT require Authorization header
  public-paths:
    - /api/auth/**
    - /actuator/**
